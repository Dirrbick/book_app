<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Book App</title>

  <link rel="stylesheet" href="../styles/reset.css">
  <link rel="stylesheet" href="../styles/base.css">
</head>

<body>
  <main>
    <%- include('../partials/navBarr.ejs') %>
    <ul>
      <div class="bookResults">
        <li><%- databaseResults.image_url%></li>
        <li><%= databaseResults.title %></li>
        <li><%= databaseResults.author %></li>
        <li><%= databaseResults.description %></li>

        <form method="post" action="/books/<%- databaseResults.id %>?_method=put">
          <input type="text" name="book_title" value="<%= databaseResults.title %>">
          <input type="text" name="book_authors" value="<%= databaseResults.author %>">
          <input type="text" name="book_description" value=" <%= databaseResults.description %>">
          <input type="text" name="book_isbn" value="<%= databaseResults.isbn %>">
          <button type="submit">Update Book</button>
        </form>

        <form method="post" action="/books/<%- databaseResults.id %>?_method=delete">
          <button type="submit">DELETE</button>
        </form>
      </div>

    </ul>





    <footer>

    </footer>
  </main>
</body>

</html>